@page
@model DataSpliderWeb.Pages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.City[0].city)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.City[0].country_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.City[0].last_update)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.City) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.city)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.country_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.last_update)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.city_id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.city_id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.city_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
